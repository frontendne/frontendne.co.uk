default:
 - 'serve'

serve:
 - 'clean:server'
 - 'svgstore'
 - 'recompile'
 - 'connect'
 - 'watch'

build:
 - 'clean:dist'
 - 'lint'
 - 'svgstore'
 - 'jekyll:dist'
 - 'sass:dist'
 - 'autoprefixer:dist'
 - 'browserify:dist'
 - 'uglify'
 - 'htmlmin'

recompile:
 - 'jekyll:dev'
 - 'browserify:dev'
 - 'sass:dev'
 - 'autoprefixer:dev'

lint:
 - 'newer:scsslint'
 - 'newer:jshint'