default:
 - 'serve'

serve:
 - 'clean:server'
 - 'svgstore'
 - 'recompile'
 - 'connect'
 - 'watch'

build:
 - 'clean:dist'
 - 'lint'
 - 'svgstore'
 - 'jekyll:dist'
 - 'sass:dist'
 - 'autoprefixer:dist'
 - 'uglify'
 - 'htmlmin'

recompile:
 - 'jekyll:dev'
 - 'copy:scripts'
 - 'sass:dev'
 - 'autoprefixer:dev'

lint:
 - 'newer:scsslint'